.header
  flexbox(flex)
  align-items(center)
  justify-content(space-between)
  position absolute
  z-index 1
  width 100%
  padding 24px

  .categoryDetails
    position absolute
    width 100%
    left 0
    text-align center
    font-weight bold
    line-height 1rem

    .categoryName
      opacity .2

  .headerControls
    svg
      margin-left size-calc(2)
